# Evaluation fixtures

Tracked text/JSON/SQL fixtures support the documented synthetic scenarios. No authentic customer dataset was confirmed in the reviewed text fixtures.

The initial publication excludes unreviewed PDF/image/video binaries, including screenshots. They remain in the original local workspace. Fixture-generation sources such as `scripts/content-synthetic-samples.py` and `scripts/documents-corpus.py` are retained; inspect their local dependencies and output paths before generating fresh files.

Recreating fixtures does not authorize model calls, uploading data to a service, or increasing historical attempt allowances. Binary output needs its own review before publication.
