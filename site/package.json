{
  "name": "ptu-bundle-stakeholder-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI Solutions Hub: a self-contained problem-first catalog and tenant pilot guide.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/build.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "test:smoke": "node tests/browser.mjs",
    "check": "npm run build && npm test && npm run test:browser",
    "preview": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.0",
    "playwright": "1.61.0"
  },
  "overrides": {
    "axe-core": "4.11.0",
    "playwright-core": "1.61.0"
  }
}
