# Continuation pass (Solutions 1-10) - report index

The full consolidated report for the continuation instruction set lives at the
bundle root, as requested:

- `PTU-Bundle-Continuation-Report.md`
- `PTU-Bundle-Continuation-Report.docx`

It is the single source of truth for this pass. Per-solution detail is in
sections 3-11 of that report; evidence paths are indexed in section 14.1.
Reports in this folder cover the **earlier** pass only.
