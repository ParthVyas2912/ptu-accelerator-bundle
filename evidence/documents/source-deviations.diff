diff --git a/App/backend-api/.dockerignore b/App/backend-api/.dockerignore
index fe1152b..a1cd36f 100644
--- a/App/backend-api/.dockerignore
+++ b/App/backend-api/.dockerignore
@@ -1,6 +1,8 @@
 **/.classpath
 **/.dockerignore
 **/.env
+**/.env.*
+**/.azure
 **/.git
 **/.gitignore
 **/.project
diff --git a/App/backend-api/Microsoft.GS.DPS.Host/DependencyConfiguration/ServiceDependencies.cs b/App/backend-api/Microsoft.GS.DPS.Host/DependencyConfiguration/ServiceDependencies.cs
index c3e5129..5b05be0 100644
--- a/App/backend-api/Microsoft.GS.DPS.Host/DependencyConfiguration/ServiceDependencies.cs
+++ b/App/backend-api/Microsoft.GS.DPS.Host/DependencyConfiguration/ServiceDependencies.cs
@@ -13,6 +13,8 @@ using Microsoft.GS.DPS.Storage.AISearch;
 using Microsoft.GS.DPSHost.AppConfiguration;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.GS.DPSHost.Helpers;
+using Azure.AI.OpenAI;
+using System.ClientModel.Primitives;
 
 namespace Microsoft.GS.DPSHost.ServiceConfiguration
 {
@@ -29,12 +31,28 @@ namespace Microsoft.GS.DPSHost.ServiceConfiguration
                 .AddSingleton<Microsoft.GS.DPS.API.UserInterface.DataCacheManager>()
                 .AddSingleton<Microsoft.SemanticKernel.Kernel>(x =>
                 {
-                    return Kernel.CreateBuilder()
+                    return EvaluationRetryConfiguration.Create(
+                        () => Kernel.CreateBuilder()
                                  .AddAzureOpenAIChatCompletion(deploymentName: builder.Configuration.GetSection("Application:AIServices:GPT-4o-mini")["ModelName"] ?? "",
                                                               endpoint: builder.Configuration.GetSection("Application:AIServices:GPT-4o-mini")["Endpoint"] ?? "",
                                                               credentials: AzureCredentialHelper.GetAzureCredential())
 
-                                 .Build();
+                                 .Build(),
+                        retries =>
+                        {
+                            var options = new AzureOpenAIClientOptions
+                            {
+                                RetryPolicy = new ClientRetryPolicy(retries)
+                            };
+                            var client = new AzureOpenAIClient(
+                                new Uri(builder.Configuration.GetSection("Application:AIServices:GPT-4o-mini")["Endpoint"] ?? ""),
+                                AzureCredentialHelper.GetAzureCredential(), options);
+                            return Kernel.CreateBuilder()
+                                .AddAzureOpenAIChatCompletion(
+                                    builder.Configuration.GetSection("Application:AIServices:GPT-4o-mini")["ModelName"] ?? "",
+                                    client)
+                                .Build();
+                        });
                 })
                 .AddSingleton<ChatSessionRepository>(x =>
                 {
diff --git a/App/frontend-app/.dockerignore b/App/frontend-app/.dockerignore
index 3c3629e..d5f1534 100644
--- a/App/frontend-app/.dockerignore
+++ b/App/frontend-app/.dockerignore
@@ -1 +1,8 @@
 node_modules
+**/.env
+**/.env.*
+**/.azure
+**/.git
+**/secrets*.json
+**/secrets*.yaml
+**/secrets*.yml
diff --git a/App/kernel-memory/.dockerignore b/App/kernel-memory/.dockerignore
index 1b0280a..72db4ae 100644
--- a/App/kernel-memory/.dockerignore
+++ b/App/kernel-memory/.dockerignore
@@ -1,5 +1,8 @@
 .dockerignore
 .env
+**/.env
+**/.env.*
+**/.azure
 .git
 .gitignore
 .vs
@@ -8,6 +11,8 @@ docker-compose.yml
 docker-compose.*.yml
 */bin
 */obj
+**/bin
+**/obj
 
 # To make sure that the local appsettings files are not copied when building the image locally.
 # These files are not in GitHub thanks to .gitignore
diff --git a/App/kernel-memory/extensions/AzureOpenAI/AzureOpenAITextEmbeddingGenerator.cs b/App/kernel-memory/extensions/AzureOpenAI/AzureOpenAITextEmbeddingGenerator.cs
index 4e1ca1e..52dd4b4 100644
--- a/App/kernel-memory/extensions/AzureOpenAI/AzureOpenAITextEmbeddingGenerator.cs
+++ b/App/kernel-memory/extensions/AzureOpenAI/AzureOpenAITextEmbeddingGenerator.cs
@@ -8,6 +8,10 @@ using System.Net.Http;
 using System.Threading;
 using System.Threading.Tasks;
 using Azure.Identity;
+using Azure;
+using Azure.AI.OpenAI;
+using Azure.Core.Pipeline;
+using Microsoft.KernelMemory.Configuration;
 using Microsoft.Extensions.Logging;
 using Microsoft.KernelMemory.AI.OpenAI;
 using Microsoft.KernelMemory.Diagnostics;
@@ -46,6 +50,36 @@ public sealed class AzureOpenAITextEmbeddingGenerator : ITextEmbeddingGenerator,
         this.MaxTokens = config.MaxTokenTotal;
         this.MaxBatchSize = config.MaxEmbeddingBatchSize;
 
+        var evaluationClient = EvaluationRetryConfiguration.Create<AzureOpenAITextEmbeddingGenerationService?>(
+            () => null,
+            retries =>
+            {
+                var options = new OpenAIClientOptions();
+                options.Retry.MaxRetries = retries;
+                if (httpClient is not null)
+                {
+                    options.Transport = new HttpClientTransport(httpClient);
+                }
+
+                var client = config.Auth switch
+                {
+                    AzureOpenAIConfig.AuthTypes.AzureIdentity =>
+                        new OpenAIClient(new Uri(config.Endpoint), new DefaultAzureCredential(), options),
+                    AzureOpenAIConfig.AuthTypes.ManualTokenCredential =>
+                        new OpenAIClient(new Uri(config.Endpoint), config.GetTokenCredential(), options),
+                    AzureOpenAIConfig.AuthTypes.APIKey =>
+                        new OpenAIClient(new Uri(config.Endpoint), new AzureKeyCredential(config.APIKey), options),
+                    _ => throw new NotImplementedException($"Azure OpenAI auth type '{config.Auth}' not available")
+                };
+                return new AzureOpenAITextEmbeddingGenerationService(
+                    config.Deployment, client, config.Deployment, loggerFactory, config.EmbeddingDimensions);
+            });
+        if (evaluationClient is not null)
+        {
+            this._client = evaluationClient;
+            return;
+        }
+
         switch (config.Auth)
         {
             case AzureOpenAIConfig.AuthTypes.AzureIdentity:
diff --git a/App/kernel-memory/service/Core/Handlers/KeywordExtractingHandler.cs b/App/kernel-memory/service/Core/Handlers/KeywordExtractingHandler.cs
index fb1ca92..3c8f6d5 100644
--- a/App/kernel-memory/service/Core/Handlers/KeywordExtractingHandler.cs
+++ b/App/kernel-memory/service/Core/Handlers/KeywordExtractingHandler.cs
@@ -6,6 +6,8 @@ using System.Text.Json;
 using System.Threading;
 using System.Threading.Tasks;
 using Azure.Identity;
+using Azure.AI.OpenAI;
+using Microsoft.KernelMemory.Configuration;
 using Microsoft.Extensions.Logging;
 using Microsoft.KernelMemory.Diagnostics;
 using Microsoft.KernelMemory.Pipeline;
@@ -36,11 +38,24 @@ public sealed class KeywordExtractingHandler : IPipelineStepHandler
         this._config = config;
 
         //init Semantic Kernel
-        this._kernel = Kernel.CreateBuilder()
+        this._kernel = EvaluationRetryConfiguration.Create(
+            () => Kernel.CreateBuilder()
             .AddAzureOpenAIChatCompletion(deploymentName: (string)this._config.Services["AzureOpenAIText"]["Deployment"],
                                                 endpoint: (string)this._config.Services["AzureOpenAIText"]["Endpoint"],
                                                   credentials: new DefaultAzureCredential())
-            .Build();
+            .Build(),
+            retries =>
+            {
+                var options = new OpenAIClientOptions();
+                options.Retry.MaxRetries = retries;
+                var client = new OpenAIClient(
+                    new Uri((string)this._config.Services["AzureOpenAIText"]["Endpoint"]),
+                    new DefaultAzureCredential(), options);
+                return Kernel.CreateBuilder()
+                    .AddAzureOpenAIChatCompletion(
+                        (string)this._config.Services["AzureOpenAIText"]["Deployment"], client)
+                    .Build();
+            });
     }
 
     public async Task<(bool success, DataPipeline updatedPipeline)> InvokeAsync(DataPipeline pipeline, CancellationToken cancellationToken = default)
@@ -152,4 +167,3 @@ public sealed class KeywordExtractingHandler : IPipelineStepHandler
     }
 
 }
-
diff --git a/infra/main.bicep b/infra/main.bicep
index 69e3991..abe3c24 100644
--- a/infra/main.bicep
+++ b/infra/main.bicep
@@ -73,6 +73,13 @@ param vmAdminPassword string?
 @description('Optional. Size of the Jumpbox Virtual Machine when created. Set to custom value if enablePrivateNetworking is true.')
 param vmSize string = 'Standard_D2s_v5'
 
+@description('AKS worker SKU. The alternative is an approved availability adaptation with the same x64 4-vCPU/16-GiB envelope.')
+@allowed([
+  'Standard_D4ds_v5'
+  'Standard_D4s_v6'
+])
+param aksNodeVmSize string = 'Standard_D4ds_v5'
+
 @description('Optional. The tags to apply to all deployed Azure resources.')
 param tags resourceInput<'Microsoft.Resources/resourceGroups@2025-04-01'>.tags = {}
 
@@ -976,7 +983,7 @@ module managedCluster 'br/public:avm/res/container-service/managed-cluster:0.13.
     primaryAgentPoolProfiles: [
       {
         name: 'agentpool'
-        vmSize: 'Standard_D4ds_v5'
+        vmSize: aksNodeVmSize
         count: 2
         osType: 'Linux'
         mode: 'System'
--- /dev/null
+++ b/App/backend-api/Microsoft.GS.DPS.Host/Helpers/EvaluationRetryConfiguration.cs
@@ -0,0 +1,27 @@
+using System;
+using System.Globalization;
+
+namespace Microsoft.GS.DPSHost.Helpers;
+
+/// <summary>Explicit evaluation opt-in; absent configuration leaves the original factory untouched.</summary>
+public static class EvaluationRetryConfiguration
+{
+    public const string EnvironmentVariableName = "DKM_EVAL_MAX_MODEL_RETRIES";
+
+    public static T Create<T>(Func<T> defaultFactory, Func<int, T> evaluationFactory)
+    {
+        var value = Environment.GetEnvironmentVariable(EnvironmentVariableName);
+        if (string.IsNullOrWhiteSpace(value))
+        {
+            return defaultFactory();
+        }
+
+        if (!int.TryParse(value.Trim(), NumberStyles.None, CultureInfo.InvariantCulture, out var retries)
+            || retries < 0 || retries > 3)
+        {
+            throw new ArgumentException(EnvironmentVariableName + " must be an integer from 0 to 3.");
+        }
+
+        return evaluationFactory(retries);
+    }
+}
--- /dev/null
+++ b/App/kernel-memory/service/Abstractions/Configuration/EvaluationRetryConfiguration.cs
@@ -0,0 +1,27 @@
+using System;
+using System.Globalization;
+
+namespace Microsoft.KernelMemory.Configuration;
+
+/// <summary>Explicit evaluation opt-in; absent configuration leaves the original factory untouched.</summary>
+public static class EvaluationRetryConfiguration
+{
+    public const string EnvironmentVariableName = "DKM_EVAL_MAX_MODEL_RETRIES";
+
+    public static T Create<T>(Func<T> defaultFactory, Func<int, T> evaluationFactory)
+    {
+        var value = Environment.GetEnvironmentVariable(EnvironmentVariableName);
+        if (string.IsNullOrWhiteSpace(value))
+        {
+            return defaultFactory();
+        }
+
+        if (!int.TryParse(value.Trim(), NumberStyles.None, CultureInfo.InvariantCulture, out var retries)
+            || retries < 0 || retries > 3)
+        {
+            throw new ArgumentException(EnvironmentVariableName + " must be an integer from 0 to 3.");
+        }
+
+        return evaluationFactory(retries);
+    }
+}
